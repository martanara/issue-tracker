import React from "react";

import Wrapper from "components/Wrapper";

const NotFound = () => {
    return (
        <Wrapper>
            <>NotFound</>
        </Wrapper>
    );
};

export default NotFound;
